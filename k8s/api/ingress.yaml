apiVersion: networking.k8s.io/v1
kind: Ingress
metadata: 
  name: api
  namespace: g8s
spec:
  rules:
  - host: api.g8s.dev
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: api
            port: 
              number: 3000
